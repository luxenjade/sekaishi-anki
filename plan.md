# 世界史年代暗記アプリ（React移行・開発計画）

本計画書は、既存の `quiz-model/` (Vanilla JS版) のデザインおよび機能を踏襲しつつ、React/TypeScript + Tailwind CSS + Lucide Icons を用いて、よりモダンで堅牢な世界史年代暗記アプリを構築するための設計・開発プランです。

---

## 1. アプリケーション概要と目的

- **目的**: 世界史の重要な出来事と年号を双方向で暗記するための、効率的で視覚的に美しいWebアプリケーション。
- **特徴**:
  - **2つのクイズモード**: 「出来事から年号を当てる（記述式入力）」および「年号から出来事を当てる（4択）」をサポート。
  - **段階的学習**: 章ごと、あるいは時代区分ごとの出題範囲設定。
  - **弱点克服機能**: クイズ後に間違えた問題だけを再度解き直すスマート再挑戦モード。
  - **ハイブリッドデータソース**: Supabase APIからの動的データ取得と、ローカルモックデータによるオフライン/未設定時の自動フォールバック。
  - **プレミアムUI**: 既存の `quiz-model/` で確立されたイエロー＆ダークの美しい配色パターンを踏襲しつつ、モダンなTailwind CSSとスムーズなアニメーションでブラッシュアップ。

---

## 2. UI/UXデザイン＆コンポーネント設計

既存の `quiz.css` のデザインエッセンスをそのままReactコンポーネントに移植し、Tailwind CSSでスタイリングします。

### 配色設計（テーマカラー）

- **プライマリアクセント**: `--qz-accent: #faba40` (鮮やかなイエロー/ゴールド)
- **正解（Success）**: `--qz-correct: #16a34a` (グリーン), 背景: `rgba(22, 163, 74, 0.15)`
- **不正解（Destructive）**: `--qz-incorrect: #dc2626` (レッド), 背景: `rgba(220, 38, 38, 0.15)`
- **ライトモード**:
  - 背景: `#fafaf8`
  - カード/サーフェス: `#ffffff`
  - テキスト: `#1a1a1a` (メイン), `#5a5a5a` (サブ)
  - ボーダー: `#e0e0da`
- **ダークモード** (クラス名 `.dark` で切り替え):
  - 背景: `#141414`
  - カード/サーフェス: `#1e1e1e`
  - テキスト: `#f0f0ec` (メイン), `#9a9a90` (サブ)
  - ボーダー: `#333330`

### 画面遷移とコンポーネント構成

1.  **`Header`**:
    - アプリタイトル、サブタイトル（現在のモード）を表示。
    - 「History (ホームに戻る)」ボタンと、ダークモード切り替えトグルを配置。
2.  **`StartScreen` (スタート画面)**:
    - クイズの概要と設定。
    - **範囲選択**: 出来事→年号では「章（第1章〜第11章）」、年号→出来事では「時代区分」をセレクトボックス/アコーディオンで選択。
    - **問題数選択**: 10問, 20問, 30問, または「全問」。
    - 「クイズを開始」ボタン（ローディング中は「読み込み中...」に変更）。
3.  **`QuizScreen` (クイズ画面)**:
    - **プログレスバー**: 現在の問題数と進行状況（例: 3 / 10）。
    - **問題エリア**: 出来事名または年号を大きく見やすく表示。
    - **回答エリア**:
      - **記述入力モード**: テキスト入力（数値のみ許容）。「前221年」のような紀元前は `-221` と入力するヘルプを表示。
      - **4択モード**: 4つの選択肢ボタン。
    - **フィードバックエリア**: 回答判定後に表示。「正解！」「不正解（正しい答え: ◯◯）」を表示。「次へ」ボタンで進行。
4.  **`ResultScreen` (結果画面)**:
    - クイズ結果のスコアを表示（例: 8 / 10 正解）。
    - 間違えた問題のリストアップ（出来事、あなたの回答、正しい回答）。
    - **アクション**: 「もう一度最初から挑戦」ボタン、「間違えた問題だけで再挑戦」ボタン。

---

## 3. 機能要件とデータ構造

### クイズロジック

1.  **記述式 (出来事 → 年号)**:
    - ユーザー入力を数値にパースし、正解と一致するか判定。
    - 紀元前（B.C.）は、`is_bc` フラグに基づいて負の値（例: `-221`）として管理し、画面上は「前221年」のようにフォーマットして表示。
2.  **4択 (年号 → 出来事)**:
    - 表示されている年号に対応する正しい出来事を1つ選択肢に含める。
    - 同じ時代区分（`period`）の他のデータから、ランダムに3つの異なる出来事を抽出してダミー選択肢（Distractors）を作成。
    - シャッフルして4つの選択肢として提示。

### データモデル

TypeScript の型定義は以下の通り設計します。

```typescript
export interface HistoryQuizItem {
  id: string; // ユニークID
  event: string; // 出来事名 (例: "フランス革命")
  year: number; // 年号 (数値、B.C.の場合は絶対値)
  is_bc: boolean; // 紀元前フラグ
  chapter: string; // 出題章 (例: "第3章")
  period: string; // 時代区分 (例: "1701~1800")
}
```

### データ取得とフォールバック

- **Supabaseフェッチ**:
  - 環境変数 `VITE_SUPABASE_URL` および `VITE_SUPABASE_ANON_KEY` を利用。
  - 範囲選択に従って `chapter` または `period` でフィルタリングしフェッチ。
- **ローカルフォールバック**:
  - Supabaseの設定がない、あるいはAPIエラーが発生した場合に備え、`src/data/mockEvents.ts` に代表的な世界史の年代データ（各章・時代区分ごとに数件〜十数件、合計約100件以上）を用意。
  - エラーや未接続時には自動でモックデータに切り替えてクイズを継続する堅牢なロジックを実装。

---

## 4. セキュリティ要件と実装方針

（`mandatory-secure-web-skills` 準拠）

1.  **入力値の安全なパースとバリデーション**:
    - 年号記述入力では、正規表現 `/^-?\d+$/` による厳格なバリデーションを行い、不正な文字列（SQLインジェクションやスクリプト文字）の介入を防ぎます。
2.  **HTML/XSS 対策**:
    - 出来事や年号のレンダリングには React の標準のエスケープ機能（中括弧 `{}`）を使い、`dangerouslySetInnerHTML` は一切使用しません。
3.  **環境変数の保護**:
    - Supabaseの認証キーは `VITE_` プレフィックスを付けた公開可能（パブリック・アノンキー）なもののみをクライアントサイドに含め、データベース側で RLS（行レベルセキュリティ）を有効化し、読み取り専用（SELECT）の権限に制限します。

---

## 5. 開発ロードマップ（ステップ・バイ・ステップ）

- **Step 1: 開発計画の策定と承認** [現在のステップ]
  - 本計画書 `plan.md` を作成し、ユーザーの承認を得る。
- **Step 2: データ型とモックデータの定義**
  - `src/types/quiz.ts` の作成。
  - `src/data/mockEvents.ts` に世界史年代のテストデータを定義。
- **Step 3: Tailwind テーマとグローバルスタイルの設定**
  - `tailwind.config.js` に `qz-accent`, `qz-correct`, `qz-incorrect` などの配色とダークモード設定を追加。
  - `src/index.css` に必要なトランジションやユーティリティを定義。
- **Step 4: クイズの状態管理ロジック (State Machine) の設計**
  - クイズの進行状態（`start` -> `playing` -> `result`）や、現在の問題番号、正解数、誤答リストを保持する React Hook / Context を構築。
- **Step 5: コンポーネントの実装**
  - `StartScreen.tsx`, `QuizScreen.tsx`, `ResultScreen.tsx` などの画面コンポーネントを作成。
  - 選択肢ボタン、テキスト入力部、プログレスバーを細分化して実装。
- **Step 6: Supabase API連携とフォールバックの統合**
  - `src/lib/supabase.ts` の作成とフェッチ関数の実装。APIエラー時のモックデータ移行処理の組み込み。
- **Step 7: UIアニメーションと最終検証**
  - 回答時のフィードバック演出（正解/不正解時のカラー変化、スライド演出）の実装。
  - レスポンシブデザイン（モバイル・タブレット・デスクトップ）のテストと微調整。

---

## 6. 進捗サマリ（2026-06-28 時点）

### ✅ 完了済みフェーズ

#### フェーズA: モノリス → 機能別分割リファクタ

| #   | 項目                                                    | 状態 | 備考                                                                 |
| --- | ------------------------------------------------------- | ---- | -------------------------------------------------------------------- |
| A-1 | `App.tsx` を1222行 → 153行に分割                        | ✅   | `lib/`, `hooks/`, `types/`, `features/`, `components/layout/` へ分離 |
| A-2 | 型定義を `src/types/quiz.ts` に集約                     | ✅   | `HistoryQuizItem`, `QuizMode`, `Profile`, `ReviewItem` ほか8種       |
| A-3 | クイズロジックを `src/lib/quiz.ts` に純粋関数化         | ✅   | `shuffleArray`, `parseYearInput`, `buildChoiceOptions` ほか          |
| A-4 | 状態管理を `useQuiz` フックに集約                       | ✅   | useState 15個 → 1フック。`startQuizWithItems`(復習用)も対応          |
| A-5 | ダークモードを `useTheme` フック化 + localStorage永続化 | ✅   | 初期値: `prefers-color-scheme` 連動                                  |
| A-6 | モックデータを 25件 → 約130件に拡充                     | ✅   | 12章 × 9時代区分 × 5分野 × 8地域カバー                               |

#### フェーズB: UI微調整

| #   | 項目                                                          | 状態 | 備考                                                                 |
| --- | ------------------------------------------------------------- | ---- | -------------------------------------------------------------------- |
| B-1 | ダークモード時のホバー可読性問題を解消                        | ✅   | `dark:hover:bg-zinc-850` (タイポ) → `dark:hover:bg-zinc-800/60` ほか |
| B-2 | Stats > Review QueueのHistoryアイコン: 赤 → テーマカラー      | ✅   | `text-rose-500` → `text-amber-500`                                   |
| B-3 | Stats > Start Smart Review: 赤・発光 → テーマカラー・フラット | ✅   | `bg-rose-500 shadow-lg` → `bg-amber-500`                             |
| B-4 | Learn > クイズを開始するボタン: 発光廃止                      | ✅   | `shadow-lg shadow-amber-500/20` 削除                                 |
| B-5 | BottomNav: 半透明+ぼかし → 完全不透明                         | ✅   | `bg-white/80 backdrop-blur-md` → `bg-white`                          |

---

## 7. 次フェーズのTODO（優先度順）

### 🎯 Phase C: Supabase 基盤整備

- [ ] **C-1**: `@supabase/supabase-js` インストール
- [ ] **C-2**: `.env.example` 整備 (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
- [ ] **C-3**: `src/lib/supabase.ts` クライアント初期化
- [ ] **C-4**: `src/lib/questions.ts` リポジトリ層（`fetchQuestions()` 実装、Supabase → モックのフォールバック）

### 🔐 Phase D: 認証 (Supabase Auth + RLS)

- [ ] **D-1**: `src/contexts/AuthContext.tsx` 作成（`useAuth` フック）
- [ ] **D-2**: `src/features/auth/AuthScreen.tsx` 作成（ログイン / 新規登録 / メール確認）
- [ ] **D-3**: App.tsx で未認証時にAuthScreenのみ表示
- [ ] **D-4**: `profiles` テーブルRLS確認（本人のみSELECT/INSERT/UPDATE/DELETE）
- [ ] **D-5**: ログイン状態の `onAuthStateChange()` 監視
- [ ] **D-6**: ログアウト / アカウント削除フロー

### 👤 Phase E: プロフィール設定

- [ ] **E-1**: `useProfile` フック作成（profilesテーブルから取得・更新）
- [ ] **E-2**: 設定タブにクイズモード切替を永続化（`profiles.quiz_mode`）
- [ ] **E-3**: テーマ設定を永続化（`profiles.theme`）
- [ ] **E-4**: ユーザーネーム変更
- [ ] **E-5**: アバターアップロード（Supabase Storage `avatars` バケット）

### 🗂 Phase F: 復習機能の実DB連携

- [ ] **F-1**: `useReview` フック作成（`review_items` テーブルCRUD）
- [ ] **F-2**: クイズ結果の不正解問題を `review_items` に自動upsert
- [ ] **F-3**: Stats画面のReview QueueをDBから取得
- [ ] **F-4**: 復習クイズ正解時に `review_items` から削除
- [ ] **F-5**: `review_count` / `last_reviewed_at` の更新

### 📝 Phase G: 投稿機能の実DB連携

- [ ] **G-1**: `wh_regions` テーブルから地域一覧を動的取得
- [ ] **G-2**: 投稿フォームを `wh_submissions` に INSERT
- [ ] **G-3**: 重複年号チェック（同じyearで既存問題があれば警告）
- [ ] **G-4**: 投稿履歴表示（pending / approved / rejected）

### 📊 Phase H: 統計タブの実データ化

- [ ] **H-1**: `profiles` から total_answered / total_correct / streak を取得
- [ ] **H-2**: `field_stats` から分野別正解率を取得 → 横バーグラフ
- [ ] **H-3**: recharts で推移グラフを追加
- [ ] **H-4**: 連続学習日数（現在・最高）の実装
- [ ] **H-5**: ランク制度（`rank_points` ベース）の実装

### 📱 Phase I: PWA対応

- [ ] **I-1**: `vite-plugin-pwa` インストール
- [ ] **I-2**: `vite.config.ts` にプラグイン設定（Workbox）
- [ ] **I-3**: `manifest.webmanifest` の整備（アイコン、テーマカラー、display）
- [ ] **I-4**: オフライン対応（問題データ / 過去クイズ結果のキャッシュ戦略）
- [ ] **I-5**: インストールプロンプトの実装

### 🚀 Phase J: デプロイ・ビルド検証

- [ ] **J-1**: `pnpm build` でバンドルサイズ確認
- [ ] **J-2**: `netlify.toml` のリダイレクト設定確認（`_redirects` 含む）
- [ ] **J-3**: Lighthouse スコア測定（PWA / Performance / Accessibility）
- [ ] **J-4**: 本番環境変数（Supabase URL/Key）のNetlify設定手順をREADMEに記載
- [ ] **J-5**: CIでビルド・型チェック・lintを自動化（任意）

---

## 8. 補足メモ

- **現状の動作確認**: クイズ機能（範囲選択・2モード・フィードバック・結果・復習）、タブ切替、ダークモード、すべてブラウザで動作確認済み
- **データ規模**: 約130件のモックデータでフォールバック対応。Supabase接続後も問題データ不足にはならない
- **保留中の既存ファイル**: `wh_admin/` ディレクトリの管理画面はSupabase接続後に活用予定
- **参考ドキュメント**: `idea.md` にオリジナルの Blueprint（DB設計 / RLS / ランク制度）が詳細に記載されている
