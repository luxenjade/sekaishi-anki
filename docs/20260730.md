# 世界史年代暗記 Web アプリ (sekaishi-anki)

## Supabase 認証付き本格 Web アプリ化 分析 & 要件整理レポート (2026-07-30)

本ドキュメントは、現在開発中の世界史年代暗記 Web アプリ (`sekaishi-anki`) を、**「Supabase アカウント認証付きの本格的な Web アプリケーション」** へ昇華させるために、**現状のコード資産・DB構成・ドキュメントを整理し、「余計なもの（整理・削除すべき要素）」と「足りないもの（導入・強化すべき要素）」** を網羅的に分析・まとめた仕様・ロードマップ書です。

---

## 1. 全体方針

現在アプリは React 19 + Vite + TypeScript + Tailwind CSS を使用し、`@supabase/supabase-js` が組み込まれた段階にあります。
しかし、過去の Vanilla JS 時代の遺物 (`quiz-model/`) の残存、SQL マイグレーションの分散、認証フローの考慮漏れ、`App.tsx` へのコード過剰集中など、本格的な運用プロダクトとしては改善すべき課題が多く存在します。

本プロジェクトの目指すべき姿：

1. **堅牢な Supabase 統合**: Auth (メール/OAuth)、PostgreSQL、RLS、TypeScript 自動型定義を活用したセキュリティと型安全性の両立。
2. **洗練された UX/UI**: URL ルーティング、レスポンシブデザイン、ダークモード対応、エラー通知 (Toast)、快適なアニメーション。
3. **高度な学習サポート**: 単なるクイズではなく、忘却曲線・ spaced repetition (間隔反復学習) や詳細な統計グラフによる継続的な学習促進。
4. **メンテナンス性の高い設計**: ロジックの切り出し、クリーンなコンポーネント分割、PWA/オフライン同期対応。

---

## 2. 余計なもの（削除・整理・リファクタリングすべき要素）

| #   | 項目                                   | 現状の問題点                                                                                                                                        | 対策・方針                                                                                                                    |
| --- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 1   | **`quiz-model/` ディレクトリ**         | Vanilla JS 時代の旧コード群（HTML/CSS/JS、別ジャンルのクイズ等）がそのまま残存し、リポジトリ容量を圧迫している。                                    | **全削除**。必要なロジックやデータ定義は既に `src/` 配下に移植・整理されているため不要。                                      |
| 2   | **分散した SQL スキーマ定義**          | ルート直下の `supabase.sql`（問題マスター）と `supabase/migrations/20260711000000_init.sql`（ユーザー系）にスキーマが分裂している。                 | **統合マイグレーションファイルに一本化**。Supabase CLI で一括管理可能な `supabase/migrations/` 配下に集約。                   |
| 3   | **旧ドキュメントの乱立**               | 過去の構想ドキュメント (`idea.md`, `plan.md`, `feedback-plan.md`) が散在し、仕様の二重化や混乱を生んでいる。                                        | 最新の要求・設計を本ドキュメント (`20260730.md`) および整理された `README.md` に集約し、旧ファイルはアーカイブ化または整理。  |
| 4   | **`App.tsx` へのコード集中 (660行超)** | ランク計算 (`getRank`)、ストリーク計算 (`calculateStreak`)、カテゴリー判定 (`getCategoryName`)、各種ステート管理が `App.tsx` に直接記述されている。 | ロジックを専用の Custom Hooks (`useStreak`, `useUserRank`) や ユーティリティ (`src/lib/` 配下) に**分離・リファクタリング**。 |
| 5   | **重複・混在した CSS 定義**            | `App.css` と `index.css` に Vanilla JS 時代の古い CSS 変数や重複スタイルが残っている。                                                              | スタイル定義を Tailwind CSS および一元化された CSS 変数にスクラップ＆ビルドして一貫性を確保。                                 |
| 6   | **強固すぎるモックデータ依存**         | `src/data/mockEvents.ts` へのフォールバックが常に最優先され、Supabase との接続状態やデータ通信エラー時の状態分離が曖昧。                            | モックはデモモード/テスト用に明確に分離し、Supabase 優先のデータ取得フローと接続判定を厳密化。                                |

---

## 3. 足りないもの（実装・強化すべき要素）

### ① 認証 (Supabase Auth) フローの完全化

- [ ] **メール認証・確認フローの強化**
  - 新規登録後の「メール確認待ち画面」の追加。
  - メール内リンクをクリックした際のリダイレクト・認証トークン処理 (`/auth/callback`)。
- [ ] **パスワード再設定 (Reset Password) フロー**
  - パスワード忘れ時の再設定メール送信機能および新パスワード入力画面の作成。
- [ ] **ソーシャルログイン (OAuth)**
  - Google / GitHub などのワンタップログイン連携（Supabase Auth OAuth provider 設定）。
- [ ] **保護されたルート (Protected Routes) とゲストモードの境界定義**
  - **ゲストモード**: アカウントなしでクイズお試しプレイ可能（成果は localStorage に一時保存）。
  - **認証済み**: 全データが Supabase にリアルタイム同期。ゲストからログイン時にローカル成果をアカウントへ自動統合。

### ② データベース (PostgreSQL & RLS) & 型定義の完成

- [ ] **単一の完全な Migration SQL 作成**
  - 以下のテーブルを一括定義するマイグレーションを作成：
    - `wh_dates` (問題マスター)
    - `wh_regions` (地域マスター)
    - `profiles` (ユーザープロフィール・集計データ)
    - `field_stats` (分野別成績)
    - `review_items` (復習キュー)
    - `wh_submissions` (ユーザー問題投稿)
    - `quiz_logs` (解答ごとの詳細ログ：解答日時、正誤、所要時間)
- [ ] **厳格な RLS (Row Level Security) ポリシーの設定**
  - `wh_dates` / `wh_regions`: 全ユーザー SELECT 可。INSERT/UPDATE は Admin のみ。
  - `profiles` / `review_items` / `field_stats` / `quiz_logs`: 自分の `user_id` のレコードのみ SELECT / INSERT / UPDATE / DELETE。
  - `wh_submissions`: 認証ユーザーが INSERT 可。閲覧・ステータス更新は Admin のみ。
- [ ] **Supabase CLI による TypeScript 型自動生成 (`src/types/database.types.ts`)**
  - `supabase gen types typescript` を導入し、Supabase クライアントに型を直接注入。

### ③ クイズ機能・学習アルゴリズムの高度化

- [ ] **間隔反復学習 (Spaced Repetition / SRS) アルゴリズム**
  - 正答率や前回の解答日に応じて、次回出題タイミング（1日後、3日後、7日後など）を自動計算し復習キューをスマート化。
- [ ] **問題投稿（SubmitTab）の管理者承認システム (Admin Dashboard)**
  - ユーザーから投稿された問題 (`status = 'pending'`) を管理者がレビューし、ワンクリックで承認 (`approved` → `wh_dates` へ自動登録) または却下 (`rejected`) できる UI。
- [ ] **詳細な学習アナリティクス (StatsTab)**
  - 正答率の推移グラフ (Recharts 活用)、得意・苦手分野のビジュアルマップ、日別解答数のヒートマップ。

### ④ UX / インフラ / フロントエンド設計の品質向上

- [ ] **クライアントサイドルーティング (React Router v7 / TanStack Router 等)**
  - 現状の `useState` によるタブ切り替えから、URL パラメータ (`/quiz`, `/review`, `/stats`, `/settings`, `/auth`) 対応へ変更。直リンクやブラウザ戻るボタンのサポート。
- [ ] **グローバル通知システム (Toast Notification)**
  - ログイン成功、設定保存、復習リスト追加、通信エラー等のフィードバックメッセージを画面端にスムーズに表示する Toast の導入。
- [ ] **PWA & オフライン同期 (Sync Queue)**
  - `vite-plugin-pwa` によるスタンドアロンインストール対応および Service Worker キャッシュ。
  - オフライン時の解答データを IndexedDB にキューイングし、オンライン復帰時に Supabase へ自動一括同期。
- [ ] **環境変数アサーション**
  - アプリ起動時に `VITE_SUPABASE_URL` と `VITE_SUPABASE_PB_KEY` の妥当性をチェックするアサーション処理。
- [ ] **自動テスト & CI/CD**
  - Vitest によるクイズ計算ロジック・SRSアルゴリズムのユニットテスト作成。
  - GitHub Actions による Lint, Typecheck, Build 自動化。

---

## 4. 推奨実装ロードマップ

```mermaid
graph TD
    A[Phase 1: クリーンアップ & DB再構築] --> B[Phase 2: 認証フロー & ルーティング完全化]
    B --> C[Phase 3: 復習アルゴリズム SRS & 管理者承認機能]
    C --> D[Phase 4: UX強化・PWA/オフライン同期 & CI/CD]

    subgraph Phase 1: クリーンアップ & DB再構築
        A1[quiz-model 削除]
        A2[App.tsx リファクタリング & Custom Hooks化]
        A3[統合 SQL Migration 作成 & RLS 設定]
        A4[Supabase CLI DB型生成]
    end

    subgraph Phase 2: 認証フロー & ルーティング完全化
        B1[React Router 導入]
        B2[メール確認 / パスワード再設定コールバック]
        B3[OAuth 連携追加]
        B4[ゲストデータ ➔ アカウント統合処理]
    end

    subgraph Phase 3: 復習アルゴリズム SRS & 管理者承認機能
        C1[quiz_logs テーブル追加 & 記録]
        C2[忘却曲線ベースの SRS アルゴリズム実装]
        C3[管理者用 Submit 承認画面の作成]
    end

    subgraph Phase 4: UX強化・PWA/オフライン同期 & CI/CD
        D1[Toast 通知システム導入]
        D2[vite-plugin-pwa & オフライン Sync Queue]
        D3[Vitest テスト & GitHub Actions 構築]
    end
```

---

## 5. まとめ

以上の分析に基づき、不要となったレガシーコードの整理を行い、Supabase の強力な認証・データベース機能をフル活用できるアーキテクチャへとアップグレードすることで、ユーザーにとって信頼性が高く、学習効果の極めて高い本格的な Web アプリケーションを完成させることができます。
